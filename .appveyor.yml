environment:
  matrix:
    - TOXENV: py37
install:
  - pip install tox
build: false
test_script:
  - git clone --depth 1 https://github.com/EFForg/https-everywhere
  - mv https-everywhere ..
  - tox
matrix:
  fast_finish: true
skip_branch_with_pr: true
